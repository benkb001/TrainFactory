**** URGENT **** 

*** BUGS **** 

* Fix randomly unlocking zoom
* Camera lag when teleporting, exiting interfaces, basically entity tracking is
  a bit wonky
* Not decreasing received damage after picking up +1 armor
  * Not sure if this is specific to tempArmor, try with perma armor too
* When pressing e not near a machine it still thinks we are interacting with it,
  Probably an issue with deciding if close enough to trigger interaction
* Couldn't easily reproduce but maybe not resetting worldtime passage to slow
* An enemy clipped outside the wall on the diagonal map
* If a machine is delivering and player saves/quits it doesn't deliver
* Fix somehow negative items? 
* Fix player invincibility frames shouldn't reset if they are currently invincible
  
**** TEST **** 

* Partitioning, especially GetIntersectingEntities
* ConnectCities
* Train.Move()
* Train fuel consumption
* Upgrade train fuel consumption/power
* ExitExpired 
* Elevator
* Combat
  * I'm still not sure how much to test because 
    a lot of it is feel-y instead of hard rules, 
    will need to look into this a bit more
* Test loading
  * I think we could do a BootStrap, save, and then load, and then 
    make some assertions about things working and being registered,
    like there being a valid player data entity, city entities, 
    train, etc

**** CLEAN UP **** 

* Test why slow on diagonal map, add exit to this map
* Filters should be in inventory wrap
* Remove querying world-state from ui systems
* Add test files for all classes (that have methods), 
  and mark any ICKY dependencies

**** THIS SPRINT **** 

* Make railroad purchase button a 1x1 paged layout
* Add a way to unpause train program from the regular train menu
* Add an exit ladder to the diagonal map 
* Fix bug where the player gun is not updated if they switch it 
  in the city view (maybe we just make gun equipped)

* Combat: 
  * Change armor that you can purchase from vendor to either 
    be parriers with more hp, or to increase your actual hp, 
    or both, and remove the current armor system. 
  * Add parrying, if you stop blocking at the right moment it
    will clear bullets in a small area around you. 
      * This is kinda tricky because of bullets not being 
        registered in the spatial hash, for now though 
        I think we can just have one entity that 
        queries for all the bullets and checks if any of 
        them intersect.
      * Also need to show this visually in some way, 
        could make a larger yellow box around you that 
        clears the bullets 
  * Add new move patterns: 
    * Parametric curves and switching between them
    * Move to a certain tile w/ pathfinding
      * We can start by having ground tiles which store 
        their position and their neighboring tiles, and a 
        shared reference to a list containing all tiles
      * Before moving, we can query for a ground tile that overlaps 
        with the enemy frame, and then we can use djikstra's to find 
        a random tile and from the path store a list of positions to go to, 
        then we check once player has gotten close enough to the next tile 
        and update target position to the next position, etc. 
  * Add new shoot patterns: 
    * Random shotgun w/ different bullet types possible
  * Add new bullet traits: 
    * bounce
    * Pierce
  * Add a few harder enemies and layouts for floors 25-60
  * There should be a significant chance (1/3 - 2/3?) that an enemy doesn't drop anything
  * Enemies should wait a random amount of time before they start to shoot the player

**** NEXT SPRINT **** 

* Add NPCs
  * Interacting should activate dialogue unless there is none, then if there 
    is a vendor it should open it. 
* Add dialogue 
  * NPCs should be able to store dialogues, sequences of strings. 
  * Tweak scene so we can deactivate/reactivate the elements beneath the dialogue 
  * Make it so it goes to the next dialgoue when you click on it or press e 
  * Add a system for code that runs once at the beginning of a day
    * It should basically add a 'day started' message when 
      hours = 0, minutes = 0, ticks = 0, and then delete it later in that tick. 
  * Make it so NPC dialogue gets refreshed at the beginning of the day 
* Add quests 
  * NPCs can store quests, we will need to check which ones have been completed 
    in persistent-state. We'll have to have some sort of object that keeps 
    track of quest completion status or is able to query for it, depending on 
    the quest, for those that can have progress completed before the player 
    accepts it. Quests will have some associated reward.
  * Need to add some way to prevent/deal with player forgetting to put a particular item 
  in train cuz its quite frustrating ? 

**** FUTURE FEATURES **** 

* In Layout, add a registry that allows us to specify certain fields for 
  certain movement types, ie maybe we want one robot to start going left 
  and another to start going right 
* Fix loops to grab products if there is no space for the 
  machines to craft
  * Need to have access to city depot level/stack sizes in TAL,
    or just a way to check the number of empty slots in an inv
  * Could also make WAIT accept a time in game-seconds for the 
    train to wait before leaving and pass as an OR. 
* Make it so player has to refresh the city interface to see which trains 
  have arrived, but when trains leave the button gets grayed out to 
  show the train isn't there anymore, and if refreshed the button will disappear
* Add a button to close menu
* Add a system to highlight entities if player is in interacting range
* Add warning if player is going to send a train with an empty inv
* Add some default toasts
* Catch up on PersistentState 
  * Player written programs 
* Add tutorials 
  * Syntax for TAL will def be needed 
* Add an equipment type to upgrade mobility, ie longer duration/shorter cd, 
  or more decrease from parrying 
* Make it so only cities that have a railroad that can connect them to an 
  accessible city are drawn on the map
* Add an auto-save feature
* Add new stats and equipment types: 
  * parry duration, parry base cooldown, parry decrease, movement speed 
  * Instead of just 1 armor, maybe armor, boots, and helmet,
    with helmet being for utility and boots for movement speed 
* Make it so messages can be drawn when you hover over an item 
  at a vendor to potentially buy it
* Add a way to view multiple machines/set their storage sizes at the same time
* Add Embassy and Abandoned Settlement
* Add random deals, time-limited, city-specific, require a large amount of items based on where player is at, 
  if completed give high rewards 
* Display inventory level in header
* Add an easier way for player to check which routes they have 'covered', 
  could be a list of all trains and which programs they have. 
* Combat:
    * Add some new bullet types for enemies
    * Add bullets that bounce off of walls
    * Add a way to make new bullet paths easily ( like parametric curves? )
  * Add on-death effects for enemies 
    * Add enemy that splits into two on death 
    * Add enemy that explodes on death 
  * Add way more enemy types, like 1 enemy type per floor.
    * Add a sniper
    * One shoots bouncy bullets 
    * One randomly shoots all around them 
    * One shoots in geometric shapes 
    * One shoots bullets in a curvy pattern 
    * One has bullets that circle around them 
  * Add way more layouts, like 20 per 5 floors. Try Tiled
  * Add visuals reloading, press r to reload
  * Add heat system, player can multiply the damage and the rewards, 
    each multiplier requires getting to bottom floor of the previous multiplier

**** FAR FUTURE FEATURES **** 

* Augment Sprite/DrawSystem to handle animation/spritesheets
* Add some tutorials/guides
* Use OpenFileDialog class to let players submit a program file from their cpu
* Add art, music, sounds, bosses, cutscenes, story, and clean UI

**** NICE TO HAVE LOW PRIORITY **** 

* Add a way to print the world state for debugging
* Add SpaceEven to LinearLayout
* Add a way for sprites to be drawn separately from collision box 
* Add a label to progress bar in machine interface to make it clear what it is when not moving
* TAL Upgrades: 
  * Add if statements 
  * Add toasts ? 
  * Add statements to set machine priority, storage size
  * Add expression to get time since arrival/time since departure
  * Add comments
  * Add parentheses

**** NOT SURE WE WANT BUT MAYBE **** 

* Make drawing System based